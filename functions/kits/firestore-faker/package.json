{
  "name": "firestore-faker",
  "version": "0.0.1-test7",
  "description": "Generate Fake Data for Firestore database",
  "dependencies": {
    "faker": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^2.5.3"
  },
  "peerDependencies": {
    "firebase-admin": "^5.4.0",
    "firebase-functions": "^0.7.0"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "deploy": "tsc && firebase deploy --only functions"
  },
  "main": "build/index.js",
  "engines": {
    "node": "~6.11.4"
  },
  "private": true
}
